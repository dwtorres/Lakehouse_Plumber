# Generated by LakehousePlumber - Monitoring Job for my_custom_monitor

resources:
  jobs:
    my_custom_monitor_job:
      name: my_custom_monitor_job
      max_concurrent_runs: 1
      tasks:
        - task_key: union_event_logs
          notebook_task:
            notebook_path: ${workspace.file_path}/monitoring/${bundle.target}/union_event_logs
            source: WORKSPACE
        - task_key: my_custom_monitor_pipeline
          depends_on:
            - task_key: union_event_logs
          pipeline_task:
            pipeline_id: ${resources.pipelines.my_custom_monitor_pipeline.id}
            full_refresh: false
      queue:
        enabled: true
      performance_target: STANDARD
