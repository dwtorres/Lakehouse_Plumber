# Generated by LakehousePlumber - Monitoring Job for acme_edw_event_log_monitoring

resources:
  jobs:
    acme_edw_event_log_monitoring_job:
      name: acme_edw_event_log_monitoring_job
      max_concurrent_runs: 1
      tasks:
        - task_key: union_event_logs
          notebook_task:
            notebook_path: ${workspace.file_path}/monitoring/${bundle.target}/union_event_logs
            source: WORKSPACE
      queue:
        enabled: true
      performance_target: STANDARD
