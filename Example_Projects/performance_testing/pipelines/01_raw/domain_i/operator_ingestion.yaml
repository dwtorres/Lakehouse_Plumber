pipeline: domain_i_raw
flowgroup: operator_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_operator_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/operator/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_operator_cloudfiles
    description: "Load operator from landing volume"

  - name: write_operator_raw
    type: write
    source: v_operator_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "operator_raw"

  - name: tst_operator_raw_completeness
    type: test
    test_type: completeness
    source: v_operator_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-operator-R01"

  - name: tst_operator_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_operator_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-operator-R02"
