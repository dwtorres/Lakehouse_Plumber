pipeline: domain_i_raw
flowgroup: machine_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_machine_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/machine/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_machine_cloudfiles
    description: "Load machine from landing volume"

  - name: write_machine_raw
    type: write
    source: v_machine_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "machine_raw"

  - name: tst_machine_raw_completeness
    type: test
    test_type: completeness
    source: v_machine_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-machine-R01"

  - name: tst_machine_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_machine_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-machine-R02"
