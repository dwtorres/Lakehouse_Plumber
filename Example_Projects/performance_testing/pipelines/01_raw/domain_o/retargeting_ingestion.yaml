pipeline: domain_o_raw
flowgroup: retargeting_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_retargeting_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/retargeting/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_retargeting_cloudfiles
    description: "Load retargeting from landing volume"

  - name: write_retargeting_raw
    type: write
    source: v_retargeting_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "retargeting_raw"

  - name: tst_retargeting_raw_completeness
    type: test
    test_type: completeness
    source: v_retargeting_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-retargeting-R01"

  - name: tst_retargeting_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_retargeting_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-retargeting-R02"
