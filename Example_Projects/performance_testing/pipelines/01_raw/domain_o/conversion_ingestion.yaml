pipeline: domain_o_raw
flowgroup: conversion_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_conversion_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/conversion/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_conversion_cloudfiles
    description: "Load conversion from landing volume"

  - name: write_conversion_raw
    type: write
    source: v_conversion_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "conversion_raw"

  - name: tst_conversion_raw_completeness
    type: test
    test_type: completeness
    source: v_conversion_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-conversion-R01"

  - name: tst_conversion_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_conversion_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-conversion-R02"
