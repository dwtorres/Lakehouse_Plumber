pipeline: domain_j_raw
flowgroup: outage_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_outage_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/outage/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_outage_cloudfiles
    description: "Load outage from landing volume"

  - name: write_outage_raw
    type: write
    source: v_outage_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "outage_raw"

  - name: tst_outage_raw_completeness
    type: test
    test_type: completeness
    source: v_outage_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-outage-R01"

  - name: tst_outage_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_outage_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-outage-R02"
