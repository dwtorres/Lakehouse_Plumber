pipeline: domain_j_raw
flowgroup: reading_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_reading_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/reading/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_reading_cloudfiles
    description: "Load reading from landing volume"

  - name: write_reading_raw
    type: write
    source: v_reading_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "reading_raw"

  - name: tst_reading_raw_completeness
    type: test
    test_type: completeness
    source: v_reading_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-reading-R01"

  - name: tst_reading_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_reading_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-reading-R02"
