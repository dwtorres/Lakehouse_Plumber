pipeline: domain_s_raw
flowgroup: case_production_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_case_production_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/case_production/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_case_production_cloudfiles
    description: "Load case_production from landing volume"

  - name: write_case_production_raw
    type: write
    source: v_case_production_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "case_production_raw"

  - name: tst_case_production_raw_completeness
    type: test
    test_type: completeness
    source: v_case_production_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-case_production-R01"

  - name: tst_case_production_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_case_production_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-case_production-R02"
