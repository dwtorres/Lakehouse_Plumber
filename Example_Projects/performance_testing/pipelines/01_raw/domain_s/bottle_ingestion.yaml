pipeline: domain_s_raw
flowgroup: bottle_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_bottle_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/bottle/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_bottle_cloudfiles
    description: "Load bottle from landing volume"

  - name: write_bottle_raw
    type: write
    source: v_bottle_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "bottle_raw"

  - name: tst_bottle_raw_completeness
    type: test
    test_type: completeness
    source: v_bottle_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-bottle-R01"

  - name: tst_bottle_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_bottle_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-bottle-R02"
