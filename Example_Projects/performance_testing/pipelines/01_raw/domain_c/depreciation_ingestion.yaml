pipeline: domain_c_raw
flowgroup: depreciation_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_depreciation_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/depreciation/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_depreciation_cloudfiles
    description: "Load depreciation from landing volume"

  - name: write_depreciation_raw
    type: write
    source: v_depreciation_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "depreciation_raw"

  - name: tst_depreciation_raw_completeness
    type: test
    test_type: completeness
    source: v_depreciation_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-depreciation-R01"

  - name: tst_depreciation_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_depreciation_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-depreciation-R02"
