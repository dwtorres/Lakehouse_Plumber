pipeline: domain_c_raw
flowgroup: forecast_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_forecast_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/forecast/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_forecast_cloudfiles
    description: "Load forecast from landing volume"

  - name: write_forecast_raw
    type: write
    source: v_forecast_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "forecast_raw"

  - name: tst_forecast_raw_completeness
    type: test
    test_type: completeness
    source: v_forecast_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-forecast-R01"

  - name: tst_forecast_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_forecast_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-forecast-R02"
