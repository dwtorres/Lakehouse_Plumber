pipeline: domain_c_raw
flowgroup: consolidation_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_consolidation_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/consolidation/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_consolidation_cloudfiles
    description: "Load consolidation from landing volume"

  - name: write_consolidation_raw
    type: write
    source: v_consolidation_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "consolidation_raw"

  - name: tst_consolidation_raw_completeness
    type: test
    test_type: completeness
    source: v_consolidation_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-consolidation-R01"

  - name: tst_consolidation_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_consolidation_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-consolidation-R02"
