pipeline: domain_c_raw
flowgroup: credit_note_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_credit_note_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/credit_note/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_credit_note_cloudfiles
    description: "Load credit_note from landing volume"

  - name: write_credit_note_raw
    type: write
    source: v_credit_note_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "credit_note_raw"

  - name: tst_credit_note_raw_completeness
    type: test
    test_type: completeness
    source: v_credit_note_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-credit_note-R01"

  - name: tst_credit_note_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_credit_note_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-credit_note-R02"
