pipeline: domain_k_raw
flowgroup: gate_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_gate_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/gate/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_gate_cloudfiles
    description: "Load gate from landing volume"

  - name: write_gate_raw
    type: write
    source: v_gate_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "gate_raw"

  - name: tst_gate_raw_completeness
    type: test
    test_type: completeness
    source: v_gate_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-gate-R01"

  - name: tst_gate_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_gate_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-gate-R02"
