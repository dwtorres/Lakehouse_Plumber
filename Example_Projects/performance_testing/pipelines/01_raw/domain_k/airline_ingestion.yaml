pipeline: domain_k_raw
flowgroup: airline_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_airline_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/airline/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_airline_cloudfiles
    description: "Load airline from landing volume"

  - name: write_airline_raw
    type: write
    source: v_airline_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "airline_raw"

  - name: tst_airline_raw_completeness
    type: test
    test_type: completeness
    source: v_airline_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-airline-R01"

  - name: tst_airline_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_airline_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-airline-R02"
