pipeline: domain_e_raw
flowgroup: geofence_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_geofence_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/geofence/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_geofence_cloudfiles
    description: "Load geofence from landing volume"

  - name: write_geofence_raw
    type: write
    source: v_geofence_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "geofence_raw"

  - name: tst_geofence_raw_completeness
    type: test
    test_type: completeness
    source: v_geofence_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-geofence-R01"

  - name: tst_geofence_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_geofence_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-geofence-R02"
