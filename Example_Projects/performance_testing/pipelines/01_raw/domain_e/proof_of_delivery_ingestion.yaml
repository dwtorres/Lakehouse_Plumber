pipeline: domain_e_raw
flowgroup: proof_of_delivery_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_proof_of_delivery_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/proof_of_delivery/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_proof_of_delivery_cloudfiles
    description: "Load proof_of_delivery from landing volume"

  - name: write_proof_of_delivery_raw
    type: write
    source: v_proof_of_delivery_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "proof_of_delivery_raw"

  - name: tst_proof_of_delivery_raw_completeness
    type: test
    test_type: completeness
    source: v_proof_of_delivery_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-proof_of_delivery-R01"

  - name: tst_proof_of_delivery_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_proof_of_delivery_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-proof_of_delivery-R02"
