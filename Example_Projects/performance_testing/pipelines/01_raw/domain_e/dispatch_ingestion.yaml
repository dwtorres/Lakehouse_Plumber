pipeline: domain_e_raw
flowgroup: dispatch_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_dispatch_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/dispatch/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_dispatch_cloudfiles
    description: "Load dispatch from landing volume"

  - name: write_dispatch_raw
    type: write
    source: v_dispatch_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "dispatch_raw"

  - name: tst_dispatch_raw_completeness
    type: test
    test_type: completeness
    source: v_dispatch_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-dispatch-R01"

  - name: tst_dispatch_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_dispatch_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-dispatch-R02"
