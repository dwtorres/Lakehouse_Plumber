pipeline: domain_e_raw
flowgroup: hub_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_hub_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/hub/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_hub_cloudfiles
    description: "Load hub from landing volume"

  - name: write_hub_raw
    type: write
    source: v_hub_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "hub_raw"

  - name: tst_hub_raw_completeness
    type: test
    test_type: completeness
    source: v_hub_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-hub-R01"

  - name: tst_hub_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_hub_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-hub-R02"
