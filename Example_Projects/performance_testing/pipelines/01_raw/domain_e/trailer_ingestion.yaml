pipeline: domain_e_raw
flowgroup: trailer_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_trailer_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/trailer/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_trailer_cloudfiles
    description: "Load trailer from landing volume"

  - name: write_trailer_raw
    type: write
    source: v_trailer_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "trailer_raw"

  - name: tst_trailer_raw_completeness
    type: test
    test_type: completeness
    source: v_trailer_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-trailer-R01"

  - name: tst_trailer_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_trailer_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-trailer-R02"
