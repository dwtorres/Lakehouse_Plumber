pipeline: domain_e_raw
flowgroup: depot_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_depot_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/depot/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_depot_cloudfiles
    description: "Load depot from landing volume"

  - name: write_depot_raw
    type: write
    source: v_depot_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "depot_raw"

  - name: tst_depot_raw_completeness
    type: test
    test_type: completeness
    source: v_depot_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-depot-R01"

  - name: tst_depot_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_depot_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-depot-R02"
