pipeline: domain_e_raw
flowgroup: vehicle_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_vehicle_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/vehicle/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_vehicle_cloudfiles
    description: "Load vehicle from landing volume"

  - name: write_vehicle_raw
    type: write
    source: v_vehicle_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "vehicle_raw"

  - name: tst_vehicle_raw_completeness
    type: test
    test_type: completeness
    source: v_vehicle_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-vehicle-R01"

  - name: tst_vehicle_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_vehicle_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-vehicle-R02"
