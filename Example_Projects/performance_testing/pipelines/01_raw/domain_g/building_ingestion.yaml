pipeline: domain_g_raw
flowgroup: building_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_building_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/building/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_building_cloudfiles
    description: "Load building from landing volume"

  - name: write_building_raw
    type: write
    source: v_building_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "building_raw"

  - name: tst_building_raw_completeness
    type: test
    test_type: completeness
    source: v_building_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-building-R01"

  - name: tst_building_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_building_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-building-R02"
