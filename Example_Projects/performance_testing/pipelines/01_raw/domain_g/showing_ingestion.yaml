pipeline: domain_g_raw
flowgroup: showing_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_showing_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/showing/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_showing_cloudfiles
    description: "Load showing from landing volume"

  - name: write_showing_raw
    type: write
    source: v_showing_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "showing_raw"

  - name: tst_showing_raw_completeness
    type: test
    test_type: completeness
    source: v_showing_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-showing-R01"

  - name: tst_showing_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_showing_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-showing-R02"
