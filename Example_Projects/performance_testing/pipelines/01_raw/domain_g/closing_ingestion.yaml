pipeline: domain_g_raw
flowgroup: closing_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_closing_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/closing/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_closing_cloudfiles
    description: "Load closing from landing volume"

  - name: write_closing_raw
    type: write
    source: v_closing_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "closing_raw"

  - name: tst_closing_raw_completeness
    type: test
    test_type: completeness
    source: v_closing_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-closing-R01"

  - name: tst_closing_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_closing_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-closing-R02"
