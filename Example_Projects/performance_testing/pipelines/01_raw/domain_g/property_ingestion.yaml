pipeline: domain_g_raw
flowgroup: property_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_property_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/property/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_property_cloudfiles
    description: "Load property from landing volume"

  - name: write_property_raw
    type: write
    source: v_property_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "property_raw"

  - name: tst_property_raw_completeness
    type: test
    test_type: completeness
    source: v_property_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-property-R01"

  - name: tst_property_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_property_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-property-R02"
