pipeline: domain_g_raw
flowgroup: staging_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_staging_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/staging/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_staging_cloudfiles
    description: "Load staging from landing volume"

  - name: write_staging_raw
    type: write
    source: v_staging_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "staging_raw"

  - name: tst_staging_raw_completeness
    type: test
    test_type: completeness
    source: v_staging_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-staging-R01"

  - name: tst_staging_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_staging_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-staging-R02"
