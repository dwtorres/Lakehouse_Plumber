pipeline: domain_g_raw
flowgroup: tenant_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_tenant_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/tenant/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_tenant_cloudfiles
    description: "Load tenant from landing volume"

  - name: write_tenant_raw
    type: write
    source: v_tenant_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "tenant_raw"

  - name: tst_tenant_raw_completeness
    type: test
    test_type: completeness
    source: v_tenant_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-tenant-R01"

  - name: tst_tenant_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_tenant_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-tenant-R02"
