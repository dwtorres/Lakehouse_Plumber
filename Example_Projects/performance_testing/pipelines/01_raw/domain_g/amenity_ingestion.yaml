pipeline: domain_g_raw
flowgroup: amenity_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_amenity_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/amenity/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_amenity_cloudfiles
    description: "Load amenity from landing volume"

  - name: write_amenity_raw
    type: write
    source: v_amenity_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "amenity_raw"

  - name: tst_amenity_raw_completeness
    type: test
    test_type: completeness
    source: v_amenity_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-amenity-R01"

  - name: tst_amenity_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_amenity_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-amenity-R02"
