pipeline: domain_g_raw
flowgroup: hoa_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_hoa_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/hoa/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_hoa_cloudfiles
    description: "Load hoa from landing volume"

  - name: write_hoa_raw
    type: write
    source: v_hoa_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "hoa_raw"

  - name: tst_hoa_raw_completeness
    type: test
    test_type: completeness
    source: v_hoa_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-hoa-R01"

  - name: tst_hoa_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_hoa_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-hoa-R02"
