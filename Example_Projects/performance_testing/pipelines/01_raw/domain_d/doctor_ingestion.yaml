pipeline: domain_d_raw
flowgroup: doctor_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_doctor_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/doctor/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_doctor_cloudfiles
    description: "Load doctor from landing volume"

  - name: write_doctor_raw
    type: write
    source: v_doctor_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "doctor_raw"

  - name: tst_doctor_raw_completeness
    type: test
    test_type: completeness
    source: v_doctor_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-doctor-R01"

  - name: tst_doctor_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_doctor_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-doctor-R02"
