pipeline: domain_h_raw
flowgroup: cashier_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_cashier_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/cashier/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_cashier_cloudfiles
    description: "Load cashier from landing volume"

  - name: write_cashier_raw
    type: write
    source: v_cashier_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "cashier_raw"

  - name: tst_cashier_raw_completeness
    type: test
    test_type: completeness
    source: v_cashier_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-cashier-R01"

  - name: tst_cashier_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_cashier_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-cashier-R02"
