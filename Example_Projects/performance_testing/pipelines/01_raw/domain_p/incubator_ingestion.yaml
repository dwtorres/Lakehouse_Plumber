pipeline: domain_p_raw
flowgroup: incubator_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_incubator_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/incubator/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_incubator_cloudfiles
    description: "Load incubator from landing volume"

  - name: write_incubator_raw
    type: write
    source: v_incubator_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "incubator_raw"

  - name: tst_incubator_raw_completeness
    type: test
    test_type: completeness
    source: v_incubator_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-incubator-R01"

  - name: tst_incubator_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_incubator_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-incubator-R02"
