pipeline: domain_p_raw
flowgroup: sample_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_sample_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/sample/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_sample_cloudfiles
    description: "Load sample from landing volume"

  - name: write_sample_raw
    type: write
    source: v_sample_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "sample_raw"

  - name: tst_sample_raw_completeness
    type: test
    test_type: completeness
    source: v_sample_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-sample-R01"

  - name: tst_sample_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_sample_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-sample-R02"
