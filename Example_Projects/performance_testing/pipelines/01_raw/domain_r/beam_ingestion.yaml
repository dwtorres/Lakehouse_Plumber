pipeline: domain_r_raw
flowgroup: beam_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_beam_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/beam/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_beam_cloudfiles
    description: "Load beam from landing volume"

  - name: write_beam_raw
    type: write
    source: v_beam_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "beam_raw"

  - name: tst_beam_raw_completeness
    type: test
    test_type: completeness
    source: v_beam_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-beam-R01"

  - name: tst_beam_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_beam_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-beam-R02"
