pipeline: domain_r_raw
flowgroup: ranging_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_ranging_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/ranging/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_ranging_cloudfiles
    description: "Load ranging from landing volume"

  - name: write_ranging_raw
    type: write
    source: v_ranging_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "ranging_raw"

  - name: tst_ranging_raw_completeness
    type: test
    test_type: completeness
    source: v_ranging_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-ranging-R01"

  - name: tst_ranging_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_ranging_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-ranging-R02"
