pipeline: domain_b_raw
flowgroup: competency_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_competency_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/competency/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_competency_cloudfiles
    description: "Load competency from landing volume"

  - name: write_competency_raw
    type: write
    source: v_competency_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "competency_raw"

  - name: tst_competency_raw_completeness
    type: test
    test_type: completeness
    source: v_competency_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-competency-R01"

  - name: tst_competency_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_competency_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-competency-R02"
