pipeline: domain_a_raw
flowgroup: inventory_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_inventory_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/inventory/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_inventory_cloudfiles
    description: "Load inventory from landing volume"

  - name: write_inventory_raw
    type: write
    source: v_inventory_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "inventory_raw"

  - name: tst_inventory_raw_completeness
    type: test
    test_type: completeness
    source: v_inventory_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-inventory-R01"

  - name: tst_inventory_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_inventory_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-inventory-R02"
