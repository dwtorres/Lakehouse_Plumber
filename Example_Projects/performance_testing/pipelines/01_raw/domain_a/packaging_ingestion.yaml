pipeline: domain_a_raw
flowgroup: packaging_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_packaging_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/packaging/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_packaging_cloudfiles
    description: "Load packaging from landing volume"

  - name: write_packaging_raw
    type: write
    source: v_packaging_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "packaging_raw"

  - name: tst_packaging_raw_completeness
    type: test
    test_type: completeness
    source: v_packaging_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-packaging-R01"

  - name: tst_packaging_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_packaging_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-packaging-R02"
