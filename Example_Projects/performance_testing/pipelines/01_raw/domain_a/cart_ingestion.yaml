pipeline: domain_a_raw
flowgroup: cart_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_cart_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/cart/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_cart_cloudfiles
    description: "Load cart from landing volume"

  - name: write_cart_raw
    type: write
    source: v_cart_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "cart_raw"

  - name: tst_cart_raw_completeness
    type: test
    test_type: completeness
    source: v_cart_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-cart-R01"

  - name: tst_cart_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_cart_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-cart-R02"
