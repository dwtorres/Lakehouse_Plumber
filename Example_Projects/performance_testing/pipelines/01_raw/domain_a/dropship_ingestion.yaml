pipeline: domain_a_raw
flowgroup: dropship_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_dropship_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/dropship/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_dropship_cloudfiles
    description: "Load dropship from landing volume"

  - name: write_dropship_raw
    type: write
    source: v_dropship_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "dropship_raw"

  - name: tst_dropship_raw_completeness
    type: test
    test_type: completeness
    source: v_dropship_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-dropship-R01"

  - name: tst_dropship_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_dropship_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-dropship-R02"
