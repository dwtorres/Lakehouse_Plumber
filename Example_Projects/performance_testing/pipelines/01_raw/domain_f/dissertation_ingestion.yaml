pipeline: domain_f_raw
flowgroup: dissertation_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_dissertation_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/dissertation/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_dissertation_cloudfiles
    description: "Load dissertation from landing volume"

  - name: write_dissertation_raw
    type: write
    source: v_dissertation_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "dissertation_raw"

  - name: tst_dissertation_raw_completeness
    type: test
    test_type: completeness
    source: v_dissertation_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-dissertation-R01"

  - name: tst_dissertation_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_dissertation_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-dissertation-R02"
