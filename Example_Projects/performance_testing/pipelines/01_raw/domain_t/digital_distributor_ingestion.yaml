pipeline: domain_t_raw
flowgroup: digital_distributor_ingestion
presets:
  - default_delta_properties
actions:
  - name: load_digital_distributor_cloudfiles
    type: load
    operational_metadata:
      - "_source_file_path"
      - "_processing_timestamp"
    source:
      type: cloudfiles
      path: "{landing_volume}/digital_distributor/*.parquet"
      format: parquet
      options:
        cloudFiles.format: parquet
        cloudFiles.maxFilesPerTrigger: 50
        cloudFiles.inferColumnTypes: True
        cloudFiles.schemaEvolutionMode: "addNewColumns"
        cloudFiles.rescuedDataColumn: "_rescued_data"
    target: v_digital_distributor_cloudfiles
    description: "Load digital_distributor from landing volume"

  - name: write_digital_distributor_raw
    type: write
    source: v_digital_distributor_cloudfiles
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: "digital_distributor_raw"

  - name: tst_digital_distributor_raw_completeness
    type: test
    test_type: completeness
    source: v_digital_distributor_cloudfiles
    required_columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-digital_distributor-R01"

  - name: tst_digital_distributor_raw_uniqueness
    type: test
    test_type: uniqueness
    source: v_digital_distributor_cloudfiles
    columns: [_processing_timestamp]
    on_violation: warn
    test_id: "PERF-digital_distributor-R02"
