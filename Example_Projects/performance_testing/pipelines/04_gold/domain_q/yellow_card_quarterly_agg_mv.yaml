pipeline: domain_q_gold
flowgroup: yellow_card_quarterly_agg_mv
actions:
  - name: yellow_card_quarterly_agg_load
    type: load
    source:
      type: sql
      sql: |
        SELECT
          date_trunc('quarter', _etl_timestamp) as period,
          count(*) as record_count,
          current_timestamp() as _generated_at
        FROM {catalog}.{silver_schema}.yellow_card_dim
        GROUP BY 1
    target: v_yellow_card_quarterly_agg

  - name: write_yellow_card_quarterly_agg_mv
    type: write
    source: v_yellow_card_quarterly_agg
    write_target:
      type: materialized_view
      catalog: "{catalog}"
      schema: "{gold_schema}"
      table: "yellow_card_quarterly_agg_mv"

  - name: tst_yellow_card_quarterly_agg_completeness
    type: test
    test_type: completeness
    source: v_yellow_card_quarterly_agg
    required_columns: [period, record_count]
    on_violation: warn
    test_id: "PERF-yellow_card-G01"

  - name: tst_yellow_card_quarterly_agg_uniqueness
    type: test
    test_type: uniqueness
    source: v_yellow_card_quarterly_agg
    columns: [period]
    on_violation: warn
    test_id: "PERF-yellow_card-G02"
