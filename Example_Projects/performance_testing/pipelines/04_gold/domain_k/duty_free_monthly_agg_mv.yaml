pipeline: domain_k_gold
flowgroup: duty_free_monthly_agg_mv
actions:
  - name: duty_free_monthly_agg_load
    type: load
    source:
      type: sql
      sql: |
        SELECT
          date_trunc('mon', _etl_timestamp) as period,
          count(*) as record_count,
          current_timestamp() as _generated_at
        FROM {catalog}.{silver_schema}.duty_free_fct
        GROUP BY 1
    target: v_duty_free_monthly_agg

  - name: write_duty_free_monthly_agg_mv
    type: write
    source: v_duty_free_monthly_agg
    write_target:
      type: materialized_view
      catalog: "{catalog}"
      schema: "{gold_schema}"
      table: "duty_free_monthly_agg_mv"

  - name: tst_duty_free_monthly_agg_completeness
    type: test
    test_type: completeness
    source: v_duty_free_monthly_agg
    required_columns: [period, record_count]
    on_violation: warn
    test_id: "PERF-duty_free-G01"

  - name: tst_duty_free_monthly_agg_uniqueness
    type: test
    test_type: uniqueness
    source: v_duty_free_monthly_agg
    columns: [period]
    on_violation: warn
    test_id: "PERF-duty_free-G02"
