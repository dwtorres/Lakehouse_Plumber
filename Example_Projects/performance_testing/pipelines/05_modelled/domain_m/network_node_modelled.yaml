pipeline: domain_m_modelled
flowgroup: network_node_modelled
presets:
  - default_delta_properties
actions:
  - name: network_node_silver_source
    type: load
    readMode: batch
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: network_node_dim
    target: v_network_node_silver
    description: "Load network_node from silver for modelling"

  - name: network_node_model_transform
    type: transform
    transform_type: sql
    readMode: batch
    source: v_network_node_silver
    target: v_network_node_modelled
    sql: |
      SELECT
        *,
        current_timestamp() as _modelled_at,
        'v1' as _model_version
      FROM v_network_node_silver

  - name: write_network_node_modelled
    type: write
    source: v_network_node_modelled
    write_target:
      type: materialized_view
      catalog: "{catalog}"
      schema: "{modelled_schema}"
      table: "network_node_modelled"

  - name: tst_network_node_modelled_completeness
    type: test
    test_type: completeness
    source: v_network_node_modelled
    required_columns: [_modelled_at, _model_version]
    on_violation: warn
    test_id: "PERF-network_node-M01"

  - name: tst_network_node_modelled_uniqueness
    type: test
    test_type: uniqueness
    source: v_network_node_modelled
    columns: [_modelled_at]
    on_violation: warn
    test_id: "PERF-network_node-M02"
