pipeline: domain_q_modelled
flowgroup: coach_modelled
presets:
  - default_delta_properties
actions:
  - name: coach_silver_source
    type: load
    readMode: batch
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: coach_fct
    target: v_coach_silver
    description: "Load coach from silver for modelling"

  - name: coach_model_transform
    type: transform
    transform_type: sql
    readMode: batch
    source: v_coach_silver
    target: v_coach_modelled
    sql: |
      SELECT
        *,
        current_timestamp() as _modelled_at,
        'v1' as _model_version
      FROM v_coach_silver

  - name: write_coach_modelled
    type: write
    source: v_coach_modelled
    write_target:
      type: materialized_view
      catalog: "{catalog}"
      schema: "{modelled_schema}"
      table: "coach_modelled"

  - name: tst_coach_modelled_completeness
    type: test
    test_type: completeness
    source: v_coach_modelled
    required_columns: [_modelled_at, _model_version]
    on_violation: warn
    test_id: "PERF-coach-M01"

  - name: tst_coach_modelled_uniqueness
    type: test
    test_type: uniqueness
    source: v_coach_modelled
    columns: [_modelled_at]
    on_violation: warn
    test_id: "PERF-coach-M02"
