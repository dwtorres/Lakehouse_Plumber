pipeline: domain_o_modelled
flowgroup: segment_modelled
presets:
  - default_delta_properties
actions:
  - name: segment_silver_source
    type: load
    readMode: batch
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: segment_fct
    target: v_segment_silver
    description: "Load segment from silver for modelling"

  - name: segment_model_transform
    type: transform
    transform_type: sql
    readMode: batch
    source: v_segment_silver
    target: v_segment_modelled
    sql: |
      SELECT
        *,
        current_timestamp() as _modelled_at,
        'v1' as _model_version
      FROM v_segment_silver

  - name: write_segment_modelled
    type: write
    source: v_segment_modelled
    write_target:
      type: materialized_view
      catalog: "{catalog}"
      schema: "{modelled_schema}"
      table: "segment_modelled"

  - name: tst_segment_modelled_completeness
    type: test
    test_type: completeness
    source: v_segment_modelled
    required_columns: [_modelled_at, _model_version]
    on_violation: warn
    test_id: "PERF-segment-M01"

  - name: tst_segment_modelled_uniqueness
    type: test
    test_type: uniqueness
    source: v_segment_modelled
    columns: [_modelled_at]
    on_violation: warn
    test_id: "PERF-segment-M02"
