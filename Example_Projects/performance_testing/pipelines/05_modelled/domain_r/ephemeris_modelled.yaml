pipeline: domain_r_modelled
flowgroup: ephemeris_modelled
presets:
  - default_delta_properties
actions:
  - name: ephemeris_silver_source
    type: load
    readMode: batch
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: ephemeris_fct
    target: v_ephemeris_silver
    description: "Load ephemeris from silver for modelling"

  - name: ephemeris_model_transform
    type: transform
    transform_type: sql
    readMode: batch
    source: v_ephemeris_silver
    target: v_ephemeris_modelled
    sql: |
      SELECT
        *,
        current_timestamp() as _modelled_at,
        'v1' as _model_version
      FROM v_ephemeris_silver

  - name: write_ephemeris_modelled
    type: write
    source: v_ephemeris_modelled
    write_target:
      type: materialized_view
      catalog: "{catalog}"
      schema: "{modelled_schema}"
      table: "ephemeris_modelled"

  - name: tst_ephemeris_modelled_completeness
    type: test
    test_type: completeness
    source: v_ephemeris_modelled
    required_columns: [_modelled_at, _model_version]
    on_violation: warn
    test_id: "PERF-ephemeris-M01"

  - name: tst_ephemeris_modelled_uniqueness
    type: test
    test_type: uniqueness
    source: v_ephemeris_modelled
    columns: [_modelled_at]
    on_violation: warn
    test_id: "PERF-ephemeris-M02"
