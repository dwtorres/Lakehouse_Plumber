pipeline: domain_r_modelled
flowgroup: bandwidth_alloc_modelled
presets:
  - default_delta_properties
actions:
  - name: bandwidth_alloc_silver_source
    type: load
    readMode: batch
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: bandwidth_alloc_fct
    target: v_bandwidth_alloc_silver
    description: "Load bandwidth_alloc from silver for modelling"

  - name: bandwidth_alloc_model_transform
    type: transform
    transform_type: sql
    readMode: batch
    source: v_bandwidth_alloc_silver
    target: v_bandwidth_alloc_modelled
    sql: |
      SELECT
        *,
        current_timestamp() as _modelled_at,
        'v1' as _model_version
      FROM v_bandwidth_alloc_silver

  - name: write_bandwidth_alloc_modelled
    type: write
    source: v_bandwidth_alloc_modelled
    write_target:
      type: materialized_view
      catalog: "{catalog}"
      schema: "{modelled_schema}"
      table: "bandwidth_alloc_modelled"

  - name: tst_bandwidth_alloc_modelled_completeness
    type: test
    test_type: completeness
    source: v_bandwidth_alloc_modelled
    required_columns: [_modelled_at, _model_version]
    on_violation: warn
    test_id: "PERF-bandwidth_alloc-M01"

  - name: tst_bandwidth_alloc_modelled_uniqueness
    type: test
    test_type: uniqueness
    source: v_bandwidth_alloc_modelled
    columns: [_modelled_at]
    on_violation: warn
    test_id: "PERF-bandwidth_alloc-M02"
