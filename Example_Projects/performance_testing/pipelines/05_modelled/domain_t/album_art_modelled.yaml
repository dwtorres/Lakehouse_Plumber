pipeline: domain_t_modelled
flowgroup: album_art_modelled
presets:
  - default_delta_properties
actions:
  - name: album_art_silver_source
    type: load
    readMode: batch
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: album_art_fct
    target: v_album_art_silver
    description: "Load album_art from silver for modelling"

  - name: album_art_model_transform
    type: transform
    transform_type: sql
    readMode: batch
    source: v_album_art_silver
    target: v_album_art_modelled
    sql: |
      SELECT
        *,
        current_timestamp() as _modelled_at,
        'v1' as _model_version
      FROM v_album_art_silver

  - name: write_album_art_modelled
    type: write
    source: v_album_art_modelled
    write_target:
      type: materialized_view
      catalog: "{catalog}"
      schema: "{modelled_schema}"
      table: "album_art_modelled"

  - name: tst_album_art_modelled_completeness
    type: test
    test_type: completeness
    source: v_album_art_modelled
    required_columns: [_modelled_at, _model_version]
    on_violation: warn
    test_id: "PERF-album_art-M01"

  - name: tst_album_art_modelled_uniqueness
    type: test
    test_type: uniqueness
    source: v_album_art_modelled
    columns: [_modelled_at]
    on_violation: warn
    test_id: "PERF-album_art-M02"
