pipeline: domain_t_modelled
flowgroup: release_modelled
presets:
  - default_delta_properties
actions:
  - name: release_silver_source
    type: load
    readMode: batch
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: release_fct
    target: v_release_silver
    description: "Load release from silver for modelling"

  - name: release_model_transform
    type: transform
    transform_type: sql
    readMode: batch
    source: v_release_silver
    target: v_release_modelled
    sql: |
      SELECT
        *,
        current_timestamp() as _modelled_at,
        'v1' as _model_version
      FROM v_release_silver

  - name: write_release_modelled
    type: write
    source: v_release_modelled
    write_target:
      type: materialized_view
      catalog: "{catalog}"
      schema: "{modelled_schema}"
      table: "release_modelled"

  - name: tst_release_modelled_completeness
    type: test
    test_type: completeness
    source: v_release_modelled
    required_columns: [_modelled_at, _model_version]
    on_violation: warn
    test_id: "PERF-release-M01"

  - name: tst_release_modelled_uniqueness
    type: test
    test_type: uniqueness
    source: v_release_modelled
    columns: [_modelled_at]
    on_violation: warn
    test_id: "PERF-release-M02"
