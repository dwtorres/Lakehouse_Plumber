pipeline: domain_s_modelled
flowgroup: distributor_modelled
presets:
  - default_delta_properties
actions:
  - name: distributor_silver_source
    type: load
    readMode: batch
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: distributor_fct
    target: v_distributor_silver
    description: "Load distributor from silver for modelling"

  - name: distributor_model_transform
    type: transform
    transform_type: sql
    readMode: batch
    source: v_distributor_silver
    target: v_distributor_modelled
    sql: |
      SELECT
        *,
        current_timestamp() as _modelled_at,
        'v1' as _model_version
      FROM v_distributor_silver

  - name: write_distributor_modelled
    type: write
    source: v_distributor_modelled
    write_target:
      type: materialized_view
      catalog: "{catalog}"
      schema: "{modelled_schema}"
      table: "distributor_modelled"

  - name: tst_distributor_modelled_completeness
    type: test
    test_type: completeness
    source: v_distributor_modelled
    required_columns: [_modelled_at, _model_version]
    on_violation: warn
    test_id: "PERF-distributor-M01"

  - name: tst_distributor_modelled_uniqueness
    type: test
    test_type: uniqueness
    source: v_distributor_modelled
    columns: [_modelled_at]
    on_violation: warn
    test_id: "PERF-distributor-M02"
