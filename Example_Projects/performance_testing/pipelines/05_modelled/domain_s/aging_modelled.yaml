pipeline: domain_s_modelled
flowgroup: aging_modelled
presets:
  - default_delta_properties
actions:
  - name: aging_silver_source
    type: load
    readMode: batch
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: aging_dim
    target: v_aging_silver
    description: "Load aging from silver for modelling"

  - name: aging_model_transform
    type: transform
    transform_type: sql
    readMode: batch
    source: v_aging_silver
    target: v_aging_modelled
    sql: |
      SELECT
        *,
        current_timestamp() as _modelled_at,
        'v1' as _model_version
      FROM v_aging_silver

  - name: write_aging_modelled
    type: write
    source: v_aging_modelled
    write_target:
      type: materialized_view
      catalog: "{catalog}"
      schema: "{modelled_schema}"
      table: "aging_modelled"

  - name: tst_aging_modelled_completeness
    type: test
    test_type: completeness
    source: v_aging_modelled
    required_columns: [_modelled_at, _model_version]
    on_violation: warn
    test_id: "PERF-aging-M01"

  - name: tst_aging_modelled_uniqueness
    type: test
    test_type: uniqueness
    source: v_aging_modelled
    columns: [_modelled_at]
    on_violation: warn
    test_id: "PERF-aging-M02"
