pipeline: domain_s_modelled
flowgroup: cellar_modelled
presets:
  - default_delta_properties
actions:
  - name: cellar_silver_source
    type: load
    readMode: batch
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: cellar_dim
    target: v_cellar_silver
    description: "Load cellar from silver for modelling"

  - name: cellar_model_transform
    type: transform
    transform_type: sql
    readMode: batch
    source: v_cellar_silver
    target: v_cellar_modelled
    sql: |
      SELECT
        *,
        current_timestamp() as _modelled_at,
        'v1' as _model_version
      FROM v_cellar_silver

  - name: write_cellar_modelled
    type: write
    source: v_cellar_modelled
    write_target:
      type: materialized_view
      catalog: "{catalog}"
      schema: "{modelled_schema}"
      table: "cellar_modelled"

  - name: tst_cellar_modelled_completeness
    type: test
    test_type: completeness
    source: v_cellar_modelled
    required_columns: [_modelled_at, _model_version]
    on_violation: warn
    test_id: "PERF-cellar-M01"

  - name: tst_cellar_modelled_uniqueness
    type: test
    test_type: uniqueness
    source: v_cellar_modelled
    columns: [_modelled_at]
    on_violation: warn
    test_id: "PERF-cellar-M02"
