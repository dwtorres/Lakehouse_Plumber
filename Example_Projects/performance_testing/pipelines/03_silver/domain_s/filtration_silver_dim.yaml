pipeline: domain_s_silver
flowgroup: filtration_silver_dim
presets:
  - default_delta_properties
actions:
  - name: filtration_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: filtration
    target: v_filtration_bronze
    description: "Load filtration from bronze for silver layer"

  - name: write_filtration_silver
    type: write
    source: v_filtration_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "filtration_dim"
      mode: "cdc"
      cdc_config:
        keys: ["filtration_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_filtration_silver_completeness
    type: test
    test_type: completeness
    source: v_filtration_bronze
    required_columns: [filtration_key]
    on_violation: warn
    test_id: "PERF-filtration-S01"

  - name: tst_filtration_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_filtration_bronze
    columns: [filtration_key]
    on_violation: warn
    test_id: "PERF-filtration-S02"
