pipeline: domain_m_silver
flowgroup: data_stream_silver_fct
presets:
  - default_delta_properties
actions:
  - name: data_stream_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: data_stream
    target: v_data_stream_bronze
    description: "Load data_stream from bronze for silver layer"

  - name: write_data_stream_silver
    type: write
    source: v_data_stream_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "data_stream_fct"
      mode: "cdc"
      cdc_config:
        keys: ["data_stream_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_data_stream_silver_completeness
    type: test
    test_type: completeness
    source: v_data_stream_bronze
    required_columns: [data_stream_key]
    on_violation: warn
    test_id: "PERF-data_stream-S01"

  - name: tst_data_stream_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_data_stream_bronze
    columns: [data_stream_key]
    on_violation: warn
    test_id: "PERF-data_stream-S02"
