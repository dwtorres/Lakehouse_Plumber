pipeline: domain_m_silver
flowgroup: heartbeat_silver_dim
presets:
  - default_delta_properties
actions:
  - name: heartbeat_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: heartbeat
    target: v_heartbeat_bronze
    description: "Load heartbeat from bronze for silver layer"

  - name: write_heartbeat_silver
    type: write
    source: v_heartbeat_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "heartbeat_dim"
      mode: "cdc"
      cdc_config:
        keys: ["heartbeat_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_heartbeat_silver_completeness
    type: test
    test_type: completeness
    source: v_heartbeat_bronze
    required_columns: [heartbeat_key]
    on_violation: warn
    test_id: "PERF-heartbeat-S01"

  - name: tst_heartbeat_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_heartbeat_bronze
    columns: [heartbeat_key]
    on_violation: warn
    test_id: "PERF-heartbeat-S02"
