pipeline: domain_n_silver
flowgroup: underwriter_silver_dim
presets:
  - default_delta_properties
actions:
  - name: underwriter_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: underwriter
    target: v_underwriter_bronze
    description: "Load underwriter from bronze for silver layer"

  - name: write_underwriter_silver
    type: write
    source: v_underwriter_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "underwriter_dim"
      mode: "cdc"
      cdc_config:
        keys: ["underwriter_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_underwriter_silver_completeness
    type: test
    test_type: completeness
    source: v_underwriter_bronze
    required_columns: [underwriter_key]
    on_violation: warn
    test_id: "PERF-underwriter-S01"

  - name: tst_underwriter_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_underwriter_bronze
    columns: [underwriter_key]
    on_violation: warn
    test_id: "PERF-underwriter-S02"
