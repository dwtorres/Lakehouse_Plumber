pipeline: domain_n_silver
flowgroup: cancellation_silver_dim
presets:
  - default_delta_properties
actions:
  - name: cancellation_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: cancellation
    target: v_cancellation_bronze
    description: "Load cancellation from bronze for silver layer"

  - name: write_cancellation_silver
    type: write
    source: v_cancellation_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "cancellation_dim"
      mode: "cdc"
      cdc_config:
        keys: ["cancellation_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_cancellation_silver_completeness
    type: test
    test_type: completeness
    source: v_cancellation_bronze
    required_columns: [cancellation_key]
    on_violation: warn
    test_id: "PERF-cancellation-S01"

  - name: tst_cancellation_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_cancellation_bronze
    columns: [cancellation_key]
    on_violation: warn
    test_id: "PERF-cancellation-S02"
