pipeline: domain_n_silver
flowgroup: retro_date_silver_dim
presets:
  - default_delta_properties
actions:
  - name: retro_date_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: retro_date
    target: v_retro_date_bronze
    description: "Load retro_date from bronze for silver layer"

  - name: write_retro_date_silver
    type: write
    source: v_retro_date_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "retro_date_dim"
      mode: "cdc"
      cdc_config:
        keys: ["retro_date_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_retro_date_silver_completeness
    type: test
    test_type: completeness
    source: v_retro_date_bronze
    required_columns: [retro_date_key]
    on_violation: warn
    test_id: "PERF-retro_date-S01"

  - name: tst_retro_date_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_retro_date_bronze
    columns: [retro_date_key]
    on_violation: warn
    test_id: "PERF-retro_date-S02"
