pipeline: domain_n_silver
flowgroup: agent_silver_fct
presets:
  - default_delta_properties
actions:
  - name: agent_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: agent
    target: v_agent_bronze
    description: "Load agent from bronze for silver layer"

  - name: write_agent_silver
    type: write
    source: v_agent_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "agent_fct"
      mode: "cdc"
      cdc_config:
        keys: ["agent_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_agent_silver_completeness
    type: test
    test_type: completeness
    source: v_agent_bronze
    required_columns: [agent_key]
    on_violation: warn
    test_id: "PERF-agent-S01"

  - name: tst_agent_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_agent_bronze
    columns: [agent_key]
    on_violation: warn
    test_id: "PERF-agent-S02"
