pipeline: domain_n_silver
flowgroup: investigation_silver_dim
presets:
  - default_delta_properties
actions:
  - name: investigation_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: investigation
    target: v_investigation_bronze
    description: "Load investigation from bronze for silver layer"

  - name: write_investigation_silver
    type: write
    source: v_investigation_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "investigation_dim"
      mode: "cdc"
      cdc_config:
        keys: ["investigation_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_investigation_silver_completeness
    type: test
    test_type: completeness
    source: v_investigation_bronze
    required_columns: [investigation_key]
    on_violation: warn
    test_id: "PERF-investigation-S01"

  - name: tst_investigation_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_investigation_bronze
    columns: [investigation_key]
    on_violation: warn
    test_id: "PERF-investigation-S02"
