pipeline: domain_o_silver
flowgroup: engagement_silver_dim
presets:
  - default_delta_properties
actions:
  - name: engagement_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: engagement
    target: v_engagement_bronze
    description: "Load engagement from bronze for silver layer"

  - name: write_engagement_silver
    type: write
    source: v_engagement_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "engagement_dim"
      mode: "cdc"
      cdc_config:
        keys: ["engagement_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_engagement_silver_completeness
    type: test
    test_type: completeness
    source: v_engagement_bronze
    required_columns: [engagement_key]
    on_violation: warn
    test_id: "PERF-engagement-S01"

  - name: tst_engagement_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_engagement_bronze
    columns: [engagement_key]
    on_violation: warn
    test_id: "PERF-engagement-S02"
