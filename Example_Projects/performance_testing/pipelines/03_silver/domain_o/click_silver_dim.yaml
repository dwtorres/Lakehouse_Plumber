pipeline: domain_o_silver
flowgroup: click_silver_dim
presets:
  - default_delta_properties
actions:
  - name: click_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: click
    target: v_click_bronze
    description: "Load click from bronze for silver layer"

  - name: write_click_silver
    type: write
    source: v_click_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "click_dim"
      mode: "cdc"
      cdc_config:
        keys: ["click_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_click_silver_completeness
    type: test
    test_type: completeness
    source: v_click_bronze
    required_columns: [click_key]
    on_violation: warn
    test_id: "PERF-click-S01"

  - name: tst_click_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_click_bronze
    columns: [click_key]
    on_violation: warn
    test_id: "PERF-click-S02"
