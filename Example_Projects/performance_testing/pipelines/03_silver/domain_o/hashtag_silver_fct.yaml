pipeline: domain_o_silver
flowgroup: hashtag_silver_fct
presets:
  - default_delta_properties
actions:
  - name: hashtag_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: hashtag
    target: v_hashtag_bronze
    description: "Load hashtag from bronze for silver layer"

  - name: write_hashtag_silver
    type: write
    source: v_hashtag_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "hashtag_fct"
      mode: "cdc"
      cdc_config:
        keys: ["hashtag_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_hashtag_silver_completeness
    type: test
    test_type: completeness
    source: v_hashtag_bronze
    required_columns: [hashtag_key]
    on_violation: warn
    test_id: "PERF-hashtag-S01"

  - name: tst_hashtag_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_hashtag_bronze
    columns: [hashtag_key]
    on_violation: warn
    test_id: "PERF-hashtag-S02"
