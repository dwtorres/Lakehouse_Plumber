pipeline: domain_o_silver
flowgroup: creative_silver_dim
presets:
  - default_delta_properties
actions:
  - name: creative_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: creative
    target: v_creative_bronze
    description: "Load creative from bronze for silver layer"

  - name: write_creative_silver
    type: write
    source: v_creative_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "creative_dim"
      mode: "cdc"
      cdc_config:
        keys: ["creative_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_creative_silver_completeness
    type: test
    test_type: completeness
    source: v_creative_bronze
    required_columns: [creative_key]
    on_violation: warn
    test_id: "PERF-creative-S01"

  - name: tst_creative_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_creative_bronze
    columns: [creative_key]
    on_violation: warn
    test_id: "PERF-creative-S02"
