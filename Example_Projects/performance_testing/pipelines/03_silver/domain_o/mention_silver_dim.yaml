pipeline: domain_o_silver
flowgroup: mention_silver_dim
presets:
  - default_delta_properties
actions:
  - name: mention_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: mention
    target: v_mention_bronze
    description: "Load mention from bronze for silver layer"

  - name: write_mention_silver
    type: write
    source: v_mention_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "mention_dim"
      mode: "cdc"
      cdc_config:
        keys: ["mention_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_mention_silver_completeness
    type: test
    test_type: completeness
    source: v_mention_bronze
    required_columns: [mention_key]
    on_violation: warn
    test_id: "PERF-mention-S01"

  - name: tst_mention_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_mention_bronze
    columns: [mention_key]
    on_violation: warn
    test_id: "PERF-mention-S02"
