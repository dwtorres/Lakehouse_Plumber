pipeline: domain_p_silver
flowgroup: alignment_silver_dim
presets:
  - default_delta_properties
actions:
  - name: alignment_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: alignment
    target: v_alignment_bronze
    description: "Load alignment from bronze for silver layer"

  - name: write_alignment_silver
    type: write
    source: v_alignment_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "alignment_dim"
      mode: "cdc"
      cdc_config:
        keys: ["alignment_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_alignment_silver_completeness
    type: test
    test_type: completeness
    source: v_alignment_bronze
    required_columns: [alignment_key]
    on_violation: warn
    test_id: "PERF-alignment-S01"

  - name: tst_alignment_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_alignment_bronze
    columns: [alignment_key]
    on_violation: warn
    test_id: "PERF-alignment-S02"
