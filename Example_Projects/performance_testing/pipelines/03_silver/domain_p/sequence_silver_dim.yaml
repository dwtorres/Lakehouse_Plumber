pipeline: domain_p_silver
flowgroup: sequence_silver_dim
presets:
  - default_delta_properties
actions:
  - name: sequence_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: sequence
    target: v_sequence_bronze
    description: "Load sequence from bronze for silver layer"

  - name: write_sequence_silver
    type: write
    source: v_sequence_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "sequence_dim"
      mode: "cdc"
      cdc_config:
        keys: ["sequence_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_sequence_silver_completeness
    type: test
    test_type: completeness
    source: v_sequence_bronze
    required_columns: [sequence_key]
    on_violation: warn
    test_id: "PERF-sequence-S01"

  - name: tst_sequence_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_sequence_bronze
    columns: [sequence_key]
    on_violation: warn
    test_id: "PERF-sequence-S02"
