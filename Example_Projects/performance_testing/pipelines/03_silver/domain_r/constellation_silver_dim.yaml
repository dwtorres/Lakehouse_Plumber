pipeline: domain_r_silver
flowgroup: constellation_silver_dim
presets:
  - default_delta_properties
actions:
  - name: constellation_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: constellation
    target: v_constellation_bronze
    description: "Load constellation from bronze for silver layer"

  - name: write_constellation_silver
    type: write
    source: v_constellation_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "constellation_dim"
      mode: "cdc"
      cdc_config:
        keys: ["constellation_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_constellation_silver_completeness
    type: test
    test_type: completeness
    source: v_constellation_bronze
    required_columns: [constellation_key]
    on_violation: warn
    test_id: "PERF-constellation-S01"

  - name: tst_constellation_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_constellation_bronze
    columns: [constellation_key]
    on_violation: warn
    test_id: "PERF-constellation-S02"
