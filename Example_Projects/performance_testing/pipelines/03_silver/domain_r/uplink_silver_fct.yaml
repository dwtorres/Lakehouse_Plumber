pipeline: domain_r_silver
flowgroup: uplink_silver_fct
presets:
  - default_delta_properties
actions:
  - name: uplink_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: uplink
    target: v_uplink_bronze
    description: "Load uplink from bronze for silver layer"

  - name: write_uplink_silver
    type: write
    source: v_uplink_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "uplink_fct"
      mode: "cdc"
      cdc_config:
        keys: ["uplink_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_uplink_silver_completeness
    type: test
    test_type: completeness
    source: v_uplink_bronze
    required_columns: [uplink_key]
    on_violation: warn
    test_id: "PERF-uplink-S01"

  - name: tst_uplink_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_uplink_bronze
    columns: [uplink_key]
    on_violation: warn
    test_id: "PERF-uplink-S02"
