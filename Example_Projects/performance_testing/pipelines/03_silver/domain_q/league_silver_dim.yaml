pipeline: domain_q_silver
flowgroup: league_silver_dim
presets:
  - default_delta_properties
actions:
  - name: league_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: league
    target: v_league_bronze
    description: "Load league from bronze for silver layer"

  - name: write_league_silver
    type: write
    source: v_league_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "league_dim"
      mode: "cdc"
      cdc_config:
        keys: ["league_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_league_silver_completeness
    type: test
    test_type: completeness
    source: v_league_bronze
    required_columns: [league_key]
    on_violation: warn
    test_id: "PERF-league-S01"

  - name: tst_league_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_league_bronze
    columns: [league_key]
    on_violation: warn
    test_id: "PERF-league-S02"
