pipeline: domain_q_silver
flowgroup: free_kick_silver_dim
presets:
  - default_delta_properties
actions:
  - name: free_kick_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: free_kick
    target: v_free_kick_bronze
    description: "Load free_kick from bronze for silver layer"

  - name: write_free_kick_silver
    type: write
    source: v_free_kick_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "free_kick_dim"
      mode: "cdc"
      cdc_config:
        keys: ["free_kick_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_free_kick_silver_completeness
    type: test
    test_type: completeness
    source: v_free_kick_bronze
    required_columns: [free_kick_key]
    on_violation: warn
    test_id: "PERF-free_kick-S01"

  - name: tst_free_kick_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_free_kick_bronze
    columns: [free_kick_key]
    on_violation: warn
    test_id: "PERF-free_kick-S02"
