pipeline: domain_e_silver
flowgroup: trailer_silver_dim
presets:
  - default_delta_properties
actions:
  - name: trailer_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: trailer
    target: v_trailer_bronze
    description: "Load trailer from bronze for silver layer"

  - name: write_trailer_silver
    type: write
    source: v_trailer_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "trailer_dim"
      mode: "cdc"
      cdc_config:
        keys: ["trailer_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_trailer_silver_completeness
    type: test
    test_type: completeness
    source: v_trailer_bronze
    required_columns: [trailer_key]
    on_violation: warn
    test_id: "PERF-trailer-S01"

  - name: tst_trailer_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_trailer_bronze
    columns: [trailer_key]
    on_violation: warn
    test_id: "PERF-trailer-S02"
---
pipeline: domain_e_silver
flowgroup: load_plan_silver_fct
presets:
  - default_delta_properties
actions:
  - name: load_plan_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: load_plan
    target: v_load_plan_bronze
    description: "Load load_plan from bronze for silver layer"

  - name: write_load_plan_silver
    type: write
    source: v_load_plan_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "load_plan_fct"
      mode: "cdc"
      cdc_config:
        keys: ["load_plan_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_load_plan_silver_completeness
    type: test
    test_type: completeness
    source: v_load_plan_bronze
    required_columns: [load_plan_key]
    on_violation: warn
    test_id: "PERF-load_plan-S01"

  - name: tst_load_plan_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_load_plan_bronze
    columns: [load_plan_key]
    on_violation: warn
    test_id: "PERF-load_plan-S02"
---
pipeline: domain_e_silver
flowgroup: weight_ticket_silver_dim
presets:
  - default_delta_properties
actions:
  - name: weight_ticket_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: weight_ticket
    target: v_weight_ticket_bronze
    description: "Load weight_ticket from bronze for silver layer"

  - name: write_weight_ticket_silver
    type: write
    source: v_weight_ticket_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "weight_ticket_dim"
      mode: "cdc"
      cdc_config:
        keys: ["weight_ticket_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_weight_ticket_silver_completeness
    type: test
    test_type: completeness
    source: v_weight_ticket_bronze
    required_columns: [weight_ticket_key]
    on_violation: warn
    test_id: "PERF-weight_ticket-S01"

  - name: tst_weight_ticket_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_weight_ticket_bronze
    columns: [weight_ticket_key]
    on_violation: warn
    test_id: "PERF-weight_ticket-S02"
---
pipeline: domain_e_silver
flowgroup: customs_doc_silver_dim
presets:
  - default_delta_properties
actions:
  - name: customs_doc_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: customs_doc
    target: v_customs_doc_bronze
    description: "Load customs_doc from bronze for silver layer"

  - name: write_customs_doc_silver
    type: write
    source: v_customs_doc_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "customs_doc_dim"
      mode: "cdc"
      cdc_config:
        keys: ["customs_doc_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_customs_doc_silver_completeness
    type: test
    test_type: completeness
    source: v_customs_doc_bronze
    required_columns: [customs_doc_key]
    on_violation: warn
    test_id: "PERF-customs_doc-S01"

  - name: tst_customs_doc_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_customs_doc_bronze
    columns: [customs_doc_key]
    on_violation: warn
    test_id: "PERF-customs_doc-S02"
---
pipeline: domain_e_silver
flowgroup: bill_of_lading_silver_fct
presets:
  - default_delta_properties
actions:
  - name: bill_of_lading_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: bill_of_lading
    target: v_bill_of_lading_bronze
    description: "Load bill_of_lading from bronze for silver layer"

  - name: write_bill_of_lading_silver
    type: write
    source: v_bill_of_lading_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "bill_of_lading_fct"
      mode: "cdc"
      cdc_config:
        keys: ["bill_of_lading_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_bill_of_lading_silver_completeness
    type: test
    test_type: completeness
    source: v_bill_of_lading_bronze
    required_columns: [bill_of_lading_key]
    on_violation: warn
    test_id: "PERF-bill_of_lading-S01"

  - name: tst_bill_of_lading_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_bill_of_lading_bronze
    columns: [bill_of_lading_key]
    on_violation: warn
    test_id: "PERF-bill_of_lading-S02"
---
pipeline: domain_e_silver
flowgroup: proof_of_delivery_silver_dim
presets:
  - default_delta_properties
actions:
  - name: proof_of_delivery_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: proof_of_delivery
    target: v_proof_of_delivery_bronze
    description: "Load proof_of_delivery from bronze for silver layer"

  - name: write_proof_of_delivery_silver
    type: write
    source: v_proof_of_delivery_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "proof_of_delivery_dim"
      mode: "cdc"
      cdc_config:
        keys: ["proof_of_delivery_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_proof_of_delivery_silver_completeness
    type: test
    test_type: completeness
    source: v_proof_of_delivery_bronze
    required_columns: [proof_of_delivery_key]
    on_violation: warn
    test_id: "PERF-proof_of_delivery-S01"

  - name: tst_proof_of_delivery_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_proof_of_delivery_bronze
    columns: [proof_of_delivery_key]
    on_violation: warn
    test_id: "PERF-proof_of_delivery-S02"
---
pipeline: domain_e_silver
flowgroup: damage_report_silver_dim
presets:
  - default_delta_properties
actions:
  - name: damage_report_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: damage_report
    target: v_damage_report_bronze
    description: "Load damage_report from bronze for silver layer"

  - name: write_damage_report_silver
    type: write
    source: v_damage_report_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "damage_report_dim"
      mode: "cdc"
      cdc_config:
        keys: ["damage_report_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_damage_report_silver_completeness
    type: test
    test_type: completeness
    source: v_damage_report_bronze
    required_columns: [damage_report_key]
    on_violation: warn
    test_id: "PERF-damage_report-S01"

  - name: tst_damage_report_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_damage_report_bronze
    columns: [damage_report_key]
    on_violation: warn
    test_id: "PERF-damage_report-S02"
---
pipeline: domain_e_silver
flowgroup: return_shipment_silver_fct
presets:
  - default_delta_properties
actions:
  - name: return_shipment_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: return_shipment
    target: v_return_shipment_bronze
    description: "Load return_shipment from bronze for silver layer"

  - name: write_return_shipment_silver
    type: write
    source: v_return_shipment_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "return_shipment_fct"
      mode: "cdc"
      cdc_config:
        keys: ["return_shipment_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_return_shipment_silver_completeness
    type: test
    test_type: completeness
    source: v_return_shipment_bronze
    required_columns: [return_shipment_key]
    on_violation: warn
    test_id: "PERF-return_shipment-S01"

  - name: tst_return_shipment_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_return_shipment_bronze
    columns: [return_shipment_key]
    on_violation: warn
    test_id: "PERF-return_shipment-S02"
---
pipeline: domain_e_silver
flowgroup: cross_dock_silver_dim
presets:
  - default_delta_properties
actions:
  - name: cross_dock_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: cross_dock
    target: v_cross_dock_bronze
    description: "Load cross_dock from bronze for silver layer"

  - name: write_cross_dock_silver
    type: write
    source: v_cross_dock_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "cross_dock_dim"
      mode: "cdc"
      cdc_config:
        keys: ["cross_dock_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_cross_dock_silver_completeness
    type: test
    test_type: completeness
    source: v_cross_dock_bronze
    required_columns: [cross_dock_key]
    on_violation: warn
    test_id: "PERF-cross_dock-S01"

  - name: tst_cross_dock_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_cross_dock_bronze
    columns: [cross_dock_key]
    on_violation: warn
    test_id: "PERF-cross_dock-S02"
---
pipeline: domain_e_silver
flowgroup: last_mile_silver_dim
presets:
  - default_delta_properties
actions:
  - name: last_mile_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: last_mile
    target: v_last_mile_bronze
    description: "Load last_mile from bronze for silver layer"

  - name: write_last_mile_silver
    type: write
    source: v_last_mile_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "last_mile_dim"
      mode: "cdc"
      cdc_config:
        keys: ["last_mile_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_last_mile_silver_completeness
    type: test
    test_type: completeness
    source: v_last_mile_bronze
    required_columns: [last_mile_key]
    on_violation: warn
    test_id: "PERF-last_mile-S01"

  - name: tst_last_mile_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_last_mile_bronze
    columns: [last_mile_key]
    on_violation: warn
    test_id: "PERF-last_mile-S02"
---
pipeline: domain_e_silver
flowgroup: route_optimization_silver_fct
presets:
  - default_delta_properties
actions:
  - name: route_optimization_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: route_optimization
    target: v_route_optimization_bronze
    description: "Load route_optimization from bronze for silver layer"

  - name: write_route_optimization_silver
    type: write
    source: v_route_optimization_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "route_optimization_fct"
      mode: "cdc"
      cdc_config:
        keys: ["route_optimization_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_route_optimization_silver_completeness
    type: test
    test_type: completeness
    source: v_route_optimization_bronze
    required_columns: [route_optimization_key]
    on_violation: warn
    test_id: "PERF-route_optimization-S01"

  - name: tst_route_optimization_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_route_optimization_bronze
    columns: [route_optimization_key]
    on_violation: warn
    test_id: "PERF-route_optimization-S02"
---
pipeline: domain_e_silver
flowgroup: geofence_silver_dim
presets:
  - default_delta_properties
actions:
  - name: geofence_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: geofence
    target: v_geofence_bronze
    description: "Load geofence from bronze for silver layer"

  - name: write_geofence_silver
    type: write
    source: v_geofence_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "geofence_dim"
      mode: "cdc"
      cdc_config:
        keys: ["geofence_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_geofence_silver_completeness
    type: test
    test_type: completeness
    source: v_geofence_bronze
    required_columns: [geofence_key]
    on_violation: warn
    test_id: "PERF-geofence-S01"

  - name: tst_geofence_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_geofence_bronze
    columns: [geofence_key]
    on_violation: warn
    test_id: "PERF-geofence-S02"
---
pipeline: domain_e_silver
flowgroup: speed_alert_silver_dim
presets:
  - default_delta_properties
actions:
  - name: speed_alert_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: speed_alert
    target: v_speed_alert_bronze
    description: "Load speed_alert from bronze for silver layer"

  - name: write_speed_alert_silver
    type: write
    source: v_speed_alert_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "speed_alert_dim"
      mode: "cdc"
      cdc_config:
        keys: ["speed_alert_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_speed_alert_silver_completeness
    type: test
    test_type: completeness
    source: v_speed_alert_bronze
    required_columns: [speed_alert_key]
    on_violation: warn
    test_id: "PERF-speed_alert-S01"

  - name: tst_speed_alert_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_speed_alert_bronze
    columns: [speed_alert_key]
    on_violation: warn
    test_id: "PERF-speed_alert-S02"
---
pipeline: domain_e_silver
flowgroup: idle_time_silver_fct
presets:
  - default_delta_properties
actions:
  - name: idle_time_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: idle_time
    target: v_idle_time_bronze
    description: "Load idle_time from bronze for silver layer"

  - name: write_idle_time_silver
    type: write
    source: v_idle_time_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "idle_time_fct"
      mode: "cdc"
      cdc_config:
        keys: ["idle_time_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_idle_time_silver_completeness
    type: test
    test_type: completeness
    source: v_idle_time_bronze
    required_columns: [idle_time_key]
    on_violation: warn
    test_id: "PERF-idle_time-S01"

  - name: tst_idle_time_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_idle_time_bronze
    columns: [idle_time_key]
    on_violation: warn
    test_id: "PERF-idle_time-S02"
---
pipeline: domain_e_silver
flowgroup: driver_score_silver_dim
presets:
  - default_delta_properties
actions:
  - name: driver_score_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: driver_score
    target: v_driver_score_bronze
    description: "Load driver_score from bronze for silver layer"

  - name: write_driver_score_silver
    type: write
    source: v_driver_score_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "driver_score_dim"
      mode: "cdc"
      cdc_config:
        keys: ["driver_score_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_driver_score_silver_completeness
    type: test
    test_type: completeness
    source: v_driver_score_bronze
    required_columns: [driver_score_key]
    on_violation: warn
    test_id: "PERF-driver_score-S01"

  - name: tst_driver_score_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_driver_score_bronze
    columns: [driver_score_key]
    on_violation: warn
    test_id: "PERF-driver_score-S02"
---
pipeline: domain_e_silver
flowgroup: eld_log_silver_dim
presets:
  - default_delta_properties
actions:
  - name: eld_log_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: eld_log
    target: v_eld_log_bronze
    description: "Load eld_log from bronze for silver layer"

  - name: write_eld_log_silver
    type: write
    source: v_eld_log_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "eld_log_dim"
      mode: "cdc"
      cdc_config:
        keys: ["eld_log_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_eld_log_silver_completeness
    type: test
    test_type: completeness
    source: v_eld_log_bronze
    required_columns: [eld_log_key]
    on_violation: warn
    test_id: "PERF-eld_log-S01"

  - name: tst_eld_log_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_eld_log_bronze
    columns: [eld_log_key]
    on_violation: warn
    test_id: "PERF-eld_log-S02"
---
pipeline: domain_e_silver
flowgroup: dot_inspection_silver_fct
presets:
  - default_delta_properties
actions:
  - name: dot_inspection_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: dot_inspection
    target: v_dot_inspection_bronze
    description: "Load dot_inspection from bronze for silver layer"

  - name: write_dot_inspection_silver
    type: write
    source: v_dot_inspection_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "dot_inspection_fct"
      mode: "cdc"
      cdc_config:
        keys: ["dot_inspection_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_dot_inspection_silver_completeness
    type: test
    test_type: completeness
    source: v_dot_inspection_bronze
    required_columns: [dot_inspection_key]
    on_violation: warn
    test_id: "PERF-dot_inspection-S01"

  - name: tst_dot_inspection_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_dot_inspection_bronze
    columns: [dot_inspection_key]
    on_violation: warn
    test_id: "PERF-dot_inspection-S02"
---
pipeline: domain_e_silver
flowgroup: hazmat_silver_dim
presets:
  - default_delta_properties
actions:
  - name: hazmat_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: hazmat
    target: v_hazmat_bronze
    description: "Load hazmat from bronze for silver layer"

  - name: write_hazmat_silver
    type: write
    source: v_hazmat_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "hazmat_dim"
      mode: "cdc"
      cdc_config:
        keys: ["hazmat_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_hazmat_silver_completeness
    type: test
    test_type: completeness
    source: v_hazmat_bronze
    required_columns: [hazmat_key]
    on_violation: warn
    test_id: "PERF-hazmat-S01"

  - name: tst_hazmat_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_hazmat_bronze
    columns: [hazmat_key]
    on_violation: warn
    test_id: "PERF-hazmat-S02"
---
pipeline: domain_e_silver
flowgroup: temperature_log_silver_dim
presets:
  - default_delta_properties
actions:
  - name: temperature_log_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: temperature_log
    target: v_temperature_log_bronze
    description: "Load temperature_log from bronze for silver layer"

  - name: write_temperature_log_silver
    type: write
    source: v_temperature_log_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "temperature_log_dim"
      mode: "cdc"
      cdc_config:
        keys: ["temperature_log_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_temperature_log_silver_completeness
    type: test
    test_type: completeness
    source: v_temperature_log_bronze
    required_columns: [temperature_log_key]
    on_violation: warn
    test_id: "PERF-temperature_log-S01"

  - name: tst_temperature_log_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_temperature_log_bronze
    columns: [temperature_log_key]
    on_violation: warn
    test_id: "PERF-temperature_log-S02"
---
pipeline: domain_e_silver
flowgroup: chain_of_custody_silver_fct
presets:
  - default_delta_properties
actions:
  - name: chain_of_custody_silver_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: chain_of_custody
    target: v_chain_of_custody_bronze
    description: "Load chain_of_custody from bronze for silver layer"

  - name: write_chain_of_custody_silver
    type: write
    source: v_chain_of_custody_bronze
    write_target:
      type: streaming_table
      catalog: "{catalog}"
      schema: "{silver_schema}"
      table: "chain_of_custody_fct"
      mode: "cdc"
      cdc_config:
        keys: ["chain_of_custody_key"]
        sequence_by: "_etl_timestamp"
        scd_type: 2
        ignore_null_updates: true
        track_history_except_column_list: ["_source_file_path", "_processing_timestamp", "_etl_timestamp"]

  - name: tst_chain_of_custody_silver_completeness
    type: test
    test_type: completeness
    source: v_chain_of_custody_bronze
    required_columns: [chain_of_custody_key]
    on_violation: warn
    test_id: "PERF-chain_of_custody-S01"

  - name: tst_chain_of_custody_silver_uniqueness
    type: test
    test_type: uniqueness
    source: v_chain_of_custody_bronze
    columns: [chain_of_custody_key]
    on_violation: warn
    test_id: "PERF-chain_of_custody-S02"
