pipeline: domain_b_bronze
flowgroup: succession_plan_bronze
presets:
  - default_delta_properties
actions:
  - name: succession_plan_raw_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: succession_plan_raw
    target: v_succession_plan_raw
    description: "Load succession_plan from raw schema"

  - name: succession_plan_bronze_cleanse
    type: transform
    transform_type: sql
    source: v_succession_plan_raw
    target: v_succession_plan_bronze_cleaned
    sql: |
      SELECT
        xxhash64(*) as succession_plan_key,
        *,
        current_timestamp() as _etl_timestamp
      FROM v_succession_plan_raw
      WHERE 1=1

  - name: succession_plan_bronze_dqe
    type: transform
    transform_type: data_quality
    source: v_succession_plan_bronze_cleaned
    target: v_succession_plan_bronze_dqe
    readMode: stream
    expectations_file: "expectations/generic_quality.json"
    description: "Apply data quality checks to succession_plan"

  - name: write_succession_plan_bronze
    type: write
    source: v_succession_plan_bronze_dqe
    write_target:
      create_table: true
      type: streaming_table
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: "succession_plan"

  - name: tst_succession_plan_bronze_completeness
    type: test
    test_type: completeness
    source: v_succession_plan_bronze_cleaned
    required_columns: [succession_plan_key, _etl_timestamp]
    on_violation: warn
    test_id: "PERF-succession_plan-B01"

  - name: tst_succession_plan_bronze_uniqueness
    type: test
    test_type: uniqueness
    source: v_succession_plan_bronze_cleaned
    columns: [succession_plan_key]
    on_violation: warn
    test_id: "PERF-succession_plan-B02"
