pipeline: domain_g_bronze
flowgroup: radon_test_bronze
presets:
  - default_delta_properties
actions:
  - name: radon_test_raw_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: radon_test_raw
    target: v_radon_test_raw
    description: "Load radon_test from raw schema"

  - name: radon_test_bronze_cleanse
    type: transform
    transform_type: sql
    source: v_radon_test_raw
    target: v_radon_test_bronze_cleaned
    sql: |
      SELECT
        xxhash64(*) as radon_test_key,
        *,
        current_timestamp() as _etl_timestamp
      FROM v_radon_test_raw
      WHERE 1=1

  - name: radon_test_bronze_dqe
    type: transform
    transform_type: data_quality
    source: v_radon_test_bronze_cleaned
    target: v_radon_test_bronze_dqe
    readMode: stream
    expectations_file: "expectations/generic_quality.json"
    description: "Apply data quality checks to radon_test"

  - name: write_radon_test_bronze
    type: write
    source: v_radon_test_bronze_dqe
    write_target:
      create_table: true
      type: streaming_table
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: "radon_test"

  - name: tst_radon_test_bronze_completeness
    type: test
    test_type: completeness
    source: v_radon_test_bronze_cleaned
    required_columns: [radon_test_key, _etl_timestamp]
    on_violation: warn
    test_id: "PERF-radon_test-B01"

  - name: tst_radon_test_bronze_uniqueness
    type: test
    test_type: uniqueness
    source: v_radon_test_bronze_cleaned
    columns: [radon_test_key]
    on_violation: warn
    test_id: "PERF-radon_test-B02"
