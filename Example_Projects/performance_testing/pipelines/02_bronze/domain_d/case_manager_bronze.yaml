pipeline: domain_d_bronze
flowgroup: case_manager_bronze
presets:
  - default_delta_properties
actions:
  - name: case_manager_raw_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: case_manager_raw
    target: v_case_manager_raw
    description: "Load case_manager from raw schema"

  - name: case_manager_bronze_cleanse
    type: transform
    transform_type: sql
    source: v_case_manager_raw
    target: v_case_manager_bronze_cleaned
    sql: |
      SELECT
        xxhash64(*) as case_manager_key,
        *,
        current_timestamp() as _etl_timestamp
      FROM v_case_manager_raw
      WHERE 1=1

  - name: case_manager_bronze_dqe
    type: transform
    transform_type: data_quality
    source: v_case_manager_bronze_cleaned
    target: v_case_manager_bronze_dqe
    readMode: stream
    expectations_file: "expectations/generic_quality.json"
    description: "Apply data quality checks to case_manager"

  - name: write_case_manager_bronze
    type: write
    source: v_case_manager_bronze_dqe
    write_target:
      create_table: true
      type: streaming_table
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: "case_manager"

  - name: tst_case_manager_bronze_completeness
    type: test
    test_type: completeness
    source: v_case_manager_bronze_cleaned
    required_columns: [case_manager_key, _etl_timestamp]
    on_violation: warn
    test_id: "PERF-case_manager-B01"

  - name: tst_case_manager_bronze_uniqueness
    type: test
    test_type: uniqueness
    source: v_case_manager_bronze_cleaned
    columns: [case_manager_key]
    on_violation: warn
    test_id: "PERF-case_manager-B02"
