pipeline: domain_d_bronze
flowgroup: nursing_note_bronze
presets:
  - default_delta_properties
actions:
  - name: nursing_note_raw_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: nursing_note_raw
    target: v_nursing_note_raw
    description: "Load nursing_note from raw schema"

  - name: nursing_note_bronze_cleanse
    type: transform
    transform_type: sql
    source: v_nursing_note_raw
    target: v_nursing_note_bronze_cleaned
    sql: |
      SELECT
        xxhash64(*) as nursing_note_key,
        *,
        current_timestamp() as _etl_timestamp
      FROM v_nursing_note_raw
      WHERE 1=1

  - name: nursing_note_bronze_dqe
    type: transform
    transform_type: data_quality
    source: v_nursing_note_bronze_cleaned
    target: v_nursing_note_bronze_dqe
    readMode: stream
    expectations_file: "expectations/generic_quality.json"
    description: "Apply data quality checks to nursing_note"

  - name: write_nursing_note_bronze
    type: write
    source: v_nursing_note_bronze_dqe
    write_target:
      create_table: true
      type: streaming_table
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: "nursing_note"

  - name: tst_nursing_note_bronze_completeness
    type: test
    test_type: completeness
    source: v_nursing_note_bronze_cleaned
    required_columns: [nursing_note_key, _etl_timestamp]
    on_violation: warn
    test_id: "PERF-nursing_note-B01"

  - name: tst_nursing_note_bronze_uniqueness
    type: test
    test_type: uniqueness
    source: v_nursing_note_bronze_cleaned
    columns: [nursing_note_key]
    on_violation: warn
    test_id: "PERF-nursing_note-B02"
