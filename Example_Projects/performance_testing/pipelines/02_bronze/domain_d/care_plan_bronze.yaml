pipeline: domain_d_bronze
flowgroup: care_plan_bronze
presets:
  - default_delta_properties
actions:
  - name: care_plan_raw_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: care_plan_raw
    target: v_care_plan_raw
    description: "Load care_plan from raw schema"

  - name: care_plan_bronze_cleanse
    type: transform
    transform_type: sql
    source: v_care_plan_raw
    target: v_care_plan_bronze_cleaned
    sql: |
      SELECT
        xxhash64(*) as care_plan_key,
        *,
        current_timestamp() as _etl_timestamp
      FROM v_care_plan_raw
      WHERE 1=1

  - name: care_plan_bronze_dqe
    type: transform
    transform_type: data_quality
    source: v_care_plan_bronze_cleaned
    target: v_care_plan_bronze_dqe
    readMode: stream
    expectations_file: "expectations/generic_quality.json"
    description: "Apply data quality checks to care_plan"

  - name: write_care_plan_bronze
    type: write
    source: v_care_plan_bronze_dqe
    write_target:
      create_table: true
      type: streaming_table
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: "care_plan"

  - name: tst_care_plan_bronze_completeness
    type: test
    test_type: completeness
    source: v_care_plan_bronze_cleaned
    required_columns: [care_plan_key, _etl_timestamp]
    on_violation: warn
    test_id: "PERF-care_plan-B01"

  - name: tst_care_plan_bronze_uniqueness
    type: test
    test_type: uniqueness
    source: v_care_plan_bronze_cleaned
    columns: [care_plan_key]
    on_violation: warn
    test_id: "PERF-care_plan-B02"
