pipeline: domain_d_bronze
flowgroup: diet_plan_bronze
presets:
  - default_delta_properties
actions:
  - name: diet_plan_raw_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: diet_plan_raw
    target: v_diet_plan_raw
    description: "Load diet_plan from raw schema"

  - name: diet_plan_bronze_cleanse
    type: transform
    transform_type: sql
    source: v_diet_plan_raw
    target: v_diet_plan_bronze_cleaned
    sql: |
      SELECT
        xxhash64(*) as diet_plan_key,
        *,
        current_timestamp() as _etl_timestamp
      FROM v_diet_plan_raw
      WHERE 1=1

  - name: diet_plan_bronze_dqe
    type: transform
    transform_type: data_quality
    source: v_diet_plan_bronze_cleaned
    target: v_diet_plan_bronze_dqe
    readMode: stream
    expectations_file: "expectations/generic_quality.json"
    description: "Apply data quality checks to diet_plan"

  - name: write_diet_plan_bronze
    type: write
    source: v_diet_plan_bronze_dqe
    write_target:
      create_table: true
      type: streaming_table
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: "diet_plan"

  - name: tst_diet_plan_bronze_completeness
    type: test
    test_type: completeness
    source: v_diet_plan_bronze_cleaned
    required_columns: [diet_plan_key, _etl_timestamp]
    on_violation: warn
    test_id: "PERF-diet_plan-B01"

  - name: tst_diet_plan_bronze_uniqueness
    type: test
    test_type: uniqueness
    source: v_diet_plan_bronze_cleaned
    columns: [diet_plan_key]
    on_violation: warn
    test_id: "PERF-diet_plan-B02"
