pipeline: domain_f_bronze
flowgroup: student_club_bronze
presets:
  - default_delta_properties
actions:
  - name: student_club_raw_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: student_club_raw
    target: v_student_club_raw
    description: "Load student_club from raw schema"

  - name: student_club_bronze_cleanse
    type: transform
    transform_type: sql
    source: v_student_club_raw
    target: v_student_club_bronze_cleaned
    sql: |
      SELECT
        xxhash64(*) as student_club_key,
        *,
        current_timestamp() as _etl_timestamp
      FROM v_student_club_raw
      WHERE 1=1

  - name: student_club_bronze_dqe
    type: transform
    transform_type: data_quality
    source: v_student_club_bronze_cleaned
    target: v_student_club_bronze_dqe
    readMode: stream
    expectations_file: "expectations/generic_quality.json"
    description: "Apply data quality checks to student_club"

  - name: write_student_club_bronze
    type: write
    source: v_student_club_bronze_dqe
    write_target:
      create_table: true
      type: streaming_table
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: "student_club"

  - name: tst_student_club_bronze_completeness
    type: test
    test_type: completeness
    source: v_student_club_bronze_cleaned
    required_columns: [student_club_key, _etl_timestamp]
    on_violation: warn
    test_id: "PERF-student_club-B01"

  - name: tst_student_club_bronze_uniqueness
    type: test
    test_type: uniqueness
    source: v_student_club_bronze_cleaned
    columns: [student_club_key]
    on_violation: warn
    test_id: "PERF-student_club-B02"
