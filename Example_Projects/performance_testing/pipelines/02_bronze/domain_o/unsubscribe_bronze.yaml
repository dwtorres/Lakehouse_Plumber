pipeline: domain_o_bronze
flowgroup: unsubscribe_bronze
presets:
  - default_delta_properties
actions:
  - name: unsubscribe_raw_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: unsubscribe_raw
    target: v_unsubscribe_raw
    description: "Load unsubscribe from raw schema"

  - name: unsubscribe_bronze_cleanse
    type: transform
    transform_type: sql
    source: v_unsubscribe_raw
    target: v_unsubscribe_bronze_cleaned
    sql: |
      SELECT
        xxhash64(*) as unsubscribe_key,
        *,
        current_timestamp() as _etl_timestamp
      FROM v_unsubscribe_raw
      WHERE 1=1

  - name: unsubscribe_bronze_dqe
    type: transform
    transform_type: data_quality
    source: v_unsubscribe_bronze_cleaned
    target: v_unsubscribe_bronze_dqe
    readMode: stream
    expectations_file: "expectations/generic_quality.json"
    description: "Apply data quality checks to unsubscribe"

  - name: write_unsubscribe_bronze
    type: write
    source: v_unsubscribe_bronze_dqe
    write_target:
      create_table: true
      type: streaming_table
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: "unsubscribe"

  - name: tst_unsubscribe_bronze_completeness
    type: test
    test_type: completeness
    source: v_unsubscribe_bronze_cleaned
    required_columns: [unsubscribe_key, _etl_timestamp]
    on_violation: warn
    test_id: "PERF-unsubscribe-B01"

  - name: tst_unsubscribe_bronze_uniqueness
    type: test
    test_type: uniqueness
    source: v_unsubscribe_bronze_cleaned
    columns: [unsubscribe_key]
    on_violation: warn
    test_id: "PERF-unsubscribe-B02"
