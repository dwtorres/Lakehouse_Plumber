pipeline: domain_q_bronze
flowgroup: tv_deal_bronze
presets:
  - default_delta_properties
actions:
  - name: tv_deal_raw_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: tv_deal_raw
    target: v_tv_deal_raw
    description: "Load tv_deal from raw schema"

  - name: tv_deal_bronze_cleanse
    type: transform
    transform_type: sql
    source: v_tv_deal_raw
    target: v_tv_deal_bronze_cleaned
    sql: |
      SELECT
        xxhash64(*) as tv_deal_key,
        *,
        current_timestamp() as _etl_timestamp
      FROM v_tv_deal_raw
      WHERE 1=1

  - name: tv_deal_bronze_dqe
    type: transform
    transform_type: data_quality
    source: v_tv_deal_bronze_cleaned
    target: v_tv_deal_bronze_dqe
    readMode: stream
    expectations_file: "expectations/generic_quality.json"
    description: "Apply data quality checks to tv_deal"

  - name: write_tv_deal_bronze
    type: write
    source: v_tv_deal_bronze_dqe
    write_target:
      create_table: true
      type: streaming_table
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: "tv_deal"

  - name: tst_tv_deal_bronze_completeness
    type: test
    test_type: completeness
    source: v_tv_deal_bronze_cleaned
    required_columns: [tv_deal_key, _etl_timestamp]
    on_violation: warn
    test_id: "PERF-tv_deal-B01"

  - name: tst_tv_deal_bronze_uniqueness
    type: test
    test_type: uniqueness
    source: v_tv_deal_bronze_cleaned
    columns: [tv_deal_key]
    on_violation: warn
    test_id: "PERF-tv_deal-B02"
