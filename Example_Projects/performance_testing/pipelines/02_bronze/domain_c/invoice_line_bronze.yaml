pipeline: domain_c_bronze
flowgroup: invoice_line_bronze
presets:
  - default_delta_properties
actions:
  - name: invoice_line_raw_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: invoice_line_raw
    target: v_invoice_line_raw
    description: "Load invoice_line from raw schema"

  - name: invoice_line_bronze_cleanse
    type: transform
    transform_type: sql
    source: v_invoice_line_raw
    target: v_invoice_line_bronze_cleaned
    sql: |
      SELECT
        xxhash64(*) as invoice_line_key,
        *,
        current_timestamp() as _etl_timestamp
      FROM v_invoice_line_raw
      WHERE 1=1

  - name: invoice_line_bronze_dqe
    type: transform
    transform_type: data_quality
    source: v_invoice_line_bronze_cleaned
    target: v_invoice_line_bronze_dqe
    readMode: stream
    expectations_file: "expectations/generic_quality.json"
    description: "Apply data quality checks to invoice_line"

  - name: write_invoice_line_bronze
    type: write
    source: v_invoice_line_bronze_dqe
    write_target:
      create_table: true
      type: streaming_table
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: "invoice_line"

  - name: tst_invoice_line_bronze_completeness
    type: test
    test_type: completeness
    source: v_invoice_line_bronze_cleaned
    required_columns: [invoice_line_key, _etl_timestamp]
    on_violation: warn
    test_id: "PERF-invoice_line-B01"

  - name: tst_invoice_line_bronze_uniqueness
    type: test
    test_type: uniqueness
    source: v_invoice_line_bronze_cleaned
    columns: [invoice_line_key]
    on_violation: warn
    test_id: "PERF-invoice_line-B02"
