pipeline: domain_c_bronze
flowgroup: credit_note_bronze
presets:
  - default_delta_properties
actions:
  - name: credit_note_raw_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: credit_note_raw
    target: v_credit_note_raw
    description: "Load credit_note from raw schema"

  - name: credit_note_bronze_cleanse
    type: transform
    transform_type: sql
    source: v_credit_note_raw
    target: v_credit_note_bronze_cleaned
    sql: |
      SELECT
        xxhash64(*) as credit_note_key,
        *,
        current_timestamp() as _etl_timestamp
      FROM v_credit_note_raw
      WHERE 1=1

  - name: credit_note_bronze_dqe
    type: transform
    transform_type: data_quality
    source: v_credit_note_bronze_cleaned
    target: v_credit_note_bronze_dqe
    readMode: stream
    expectations_file: "expectations/generic_quality.json"
    description: "Apply data quality checks to credit_note"

  - name: write_credit_note_bronze
    type: write
    source: v_credit_note_bronze_dqe
    write_target:
      create_table: true
      type: streaming_table
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: "credit_note"

  - name: tst_credit_note_bronze_completeness
    type: test
    test_type: completeness
    source: v_credit_note_bronze_cleaned
    required_columns: [credit_note_key, _etl_timestamp]
    on_violation: warn
    test_id: "PERF-credit_note-B01"

  - name: tst_credit_note_bronze_uniqueness
    type: test
    test_type: uniqueness
    source: v_credit_note_bronze_cleaned
    columns: [credit_note_key]
    on_violation: warn
    test_id: "PERF-credit_note-B02"
