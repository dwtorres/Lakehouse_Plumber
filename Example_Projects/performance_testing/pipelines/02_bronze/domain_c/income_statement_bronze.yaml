pipeline: domain_c_bronze
flowgroup: income_statement_bronze
presets:
  - default_delta_properties
actions:
  - name: income_statement_raw_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: income_statement_raw
    target: v_income_statement_raw
    description: "Load income_statement from raw schema"

  - name: income_statement_bronze_cleanse
    type: transform
    transform_type: sql
    source: v_income_statement_raw
    target: v_income_statement_bronze_cleaned
    sql: |
      SELECT
        xxhash64(*) as income_statement_key,
        *,
        current_timestamp() as _etl_timestamp
      FROM v_income_statement_raw
      WHERE 1=1

  - name: income_statement_bronze_dqe
    type: transform
    transform_type: data_quality
    source: v_income_statement_bronze_cleaned
    target: v_income_statement_bronze_dqe
    readMode: stream
    expectations_file: "expectations/generic_quality.json"
    description: "Apply data quality checks to income_statement"

  - name: write_income_statement_bronze
    type: write
    source: v_income_statement_bronze_dqe
    write_target:
      create_table: true
      type: streaming_table
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: "income_statement"

  - name: tst_income_statement_bronze_completeness
    type: test
    test_type: completeness
    source: v_income_statement_bronze_cleaned
    required_columns: [income_statement_key, _etl_timestamp]
    on_violation: warn
    test_id: "PERF-income_statement-B01"

  - name: tst_income_statement_bronze_uniqueness
    type: test
    test_type: uniqueness
    source: v_income_statement_bronze_cleaned
    columns: [income_statement_key]
    on_violation: warn
    test_id: "PERF-income_statement-B02"
