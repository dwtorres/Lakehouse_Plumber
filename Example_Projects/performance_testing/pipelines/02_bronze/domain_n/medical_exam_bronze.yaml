pipeline: domain_n_bronze
flowgroup: medical_exam_bronze
presets:
  - default_delta_properties
actions:
  - name: medical_exam_raw_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: medical_exam_raw
    target: v_medical_exam_raw
    description: "Load medical_exam from raw schema"

  - name: medical_exam_bronze_cleanse
    type: transform
    transform_type: sql
    source: v_medical_exam_raw
    target: v_medical_exam_bronze_cleaned
    sql: |
      SELECT
        xxhash64(*) as medical_exam_key,
        *,
        current_timestamp() as _etl_timestamp
      FROM v_medical_exam_raw
      WHERE 1=1

  - name: medical_exam_bronze_dqe
    type: transform
    transform_type: data_quality
    source: v_medical_exam_bronze_cleaned
    target: v_medical_exam_bronze_dqe
    readMode: stream
    expectations_file: "expectations/generic_quality.json"
    description: "Apply data quality checks to medical_exam"

  - name: write_medical_exam_bronze
    type: write
    source: v_medical_exam_bronze_dqe
    write_target:
      create_table: true
      type: streaming_table
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: "medical_exam"

  - name: tst_medical_exam_bronze_completeness
    type: test
    test_type: completeness
    source: v_medical_exam_bronze_cleaned
    required_columns: [medical_exam_key, _etl_timestamp]
    on_violation: warn
    test_id: "PERF-medical_exam-B01"

  - name: tst_medical_exam_bronze_uniqueness
    type: test
    test_type: uniqueness
    source: v_medical_exam_bronze_cleaned
    columns: [medical_exam_key]
    on_violation: warn
    test_id: "PERF-medical_exam-B02"
