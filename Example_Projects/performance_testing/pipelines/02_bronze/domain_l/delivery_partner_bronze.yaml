pipeline: domain_l_bronze
flowgroup: delivery_partner_bronze
presets:
  - default_delta_properties
actions:
  - name: delivery_partner_raw_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: delivery_partner_raw
    target: v_delivery_partner_raw
    description: "Load delivery_partner from raw schema"

  - name: delivery_partner_bronze_cleanse
    type: transform
    transform_type: sql
    source: v_delivery_partner_raw
    target: v_delivery_partner_bronze_cleaned
    sql: |
      SELECT
        xxhash64(*) as delivery_partner_key,
        *,
        current_timestamp() as _etl_timestamp
      FROM v_delivery_partner_raw
      WHERE 1=1

  - name: delivery_partner_bronze_dqe
    type: transform
    transform_type: data_quality
    source: v_delivery_partner_bronze_cleaned
    target: v_delivery_partner_bronze_dqe
    readMode: stream
    expectations_file: "expectations/generic_quality.json"
    description: "Apply data quality checks to delivery_partner"

  - name: write_delivery_partner_bronze
    type: write
    source: v_delivery_partner_bronze_dqe
    write_target:
      create_table: true
      type: streaming_table
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: "delivery_partner"

  - name: tst_delivery_partner_bronze_completeness
    type: test
    test_type: completeness
    source: v_delivery_partner_bronze_cleaned
    required_columns: [delivery_partner_key, _etl_timestamp]
    on_violation: warn
    test_id: "PERF-delivery_partner-B01"

  - name: tst_delivery_partner_bronze_uniqueness
    type: test
    test_type: uniqueness
    source: v_delivery_partner_bronze_cleaned
    columns: [delivery_partner_key]
    on_violation: warn
    test_id: "PERF-delivery_partner-B02"
