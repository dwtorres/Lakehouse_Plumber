pipeline: domain_j_bronze
flowgroup: distribution_line_bronze
presets:
  - default_delta_properties
actions:
  - name: distribution_line_raw_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: distribution_line_raw
    target: v_distribution_line_raw
    description: "Load distribution_line from raw schema"

  - name: distribution_line_bronze_cleanse
    type: transform
    transform_type: sql
    source: v_distribution_line_raw
    target: v_distribution_line_bronze_cleaned
    sql: |
      SELECT
        xxhash64(*) as distribution_line_key,
        *,
        current_timestamp() as _etl_timestamp
      FROM v_distribution_line_raw
      WHERE 1=1

  - name: distribution_line_bronze_dqe
    type: transform
    transform_type: data_quality
    source: v_distribution_line_bronze_cleaned
    target: v_distribution_line_bronze_dqe
    readMode: stream
    expectations_file: "expectations/generic_quality.json"
    description: "Apply data quality checks to distribution_line"

  - name: write_distribution_line_bronze
    type: write
    source: v_distribution_line_bronze_dqe
    write_target:
      create_table: true
      type: streaming_table
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: "distribution_line"

  - name: tst_distribution_line_bronze_completeness
    type: test
    test_type: completeness
    source: v_distribution_line_bronze_cleaned
    required_columns: [distribution_line_key, _etl_timestamp]
    on_violation: warn
    test_id: "PERF-distribution_line-B01"

  - name: tst_distribution_line_bronze_uniqueness
    type: test
    test_type: uniqueness
    source: v_distribution_line_bronze_cleaned
    columns: [distribution_line_key]
    on_violation: warn
    test_id: "PERF-distribution_line-B02"
