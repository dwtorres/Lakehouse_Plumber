pipeline: domain_a_bronze
flowgroup: variant_bronze
presets:
  - default_delta_properties
actions:
  - name: variant_raw_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: variant_raw
    target: v_variant_raw
    description: "Load variant from raw schema"

  - name: variant_bronze_cleanse
    type: transform
    transform_type: sql
    source: v_variant_raw
    target: v_variant_bronze_cleaned
    sql: |
      SELECT
        xxhash64(*) as variant_key,
        *,
        current_timestamp() as _etl_timestamp
      FROM v_variant_raw
      WHERE 1=1

  - name: variant_bronze_dqe
    type: transform
    transform_type: data_quality
    source: v_variant_bronze_cleaned
    target: v_variant_bronze_dqe
    readMode: stream
    expectations_file: "expectations/generic_quality.json"
    description: "Apply data quality checks to variant"

  - name: write_variant_bronze
    type: write
    source: v_variant_bronze_dqe
    write_target:
      create_table: true
      type: streaming_table
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: "variant"

  - name: tst_variant_bronze_completeness
    type: test
    test_type: completeness
    source: v_variant_bronze_cleaned
    required_columns: [variant_key, _etl_timestamp]
    on_violation: warn
    test_id: "PERF-variant-B01"

  - name: tst_variant_bronze_uniqueness
    type: test
    test_type: uniqueness
    source: v_variant_bronze_cleaned
    columns: [variant_key]
    on_violation: warn
    test_id: "PERF-variant-B02"
