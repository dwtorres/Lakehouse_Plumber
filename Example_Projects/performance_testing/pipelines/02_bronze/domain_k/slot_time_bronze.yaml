pipeline: domain_k_bronze
flowgroup: slot_time_bronze
presets:
  - default_delta_properties
actions:
  - name: slot_time_raw_load
    type: load
    operational_metadata: ["_processing_timestamp"]
    readMode: stream
    source:
      type: delta
      catalog: "{catalog}"
      schema: "{raw_schema}"
      table: slot_time_raw
    target: v_slot_time_raw
    description: "Load slot_time from raw schema"

  - name: slot_time_bronze_cleanse
    type: transform
    transform_type: sql
    source: v_slot_time_raw
    target: v_slot_time_bronze_cleaned
    sql: |
      SELECT
        xxhash64(*) as slot_time_key,
        *,
        current_timestamp() as _etl_timestamp
      FROM v_slot_time_raw
      WHERE 1=1

  - name: slot_time_bronze_dqe
    type: transform
    transform_type: data_quality
    source: v_slot_time_bronze_cleaned
    target: v_slot_time_bronze_dqe
    readMode: stream
    expectations_file: "expectations/generic_quality.json"
    description: "Apply data quality checks to slot_time"

  - name: write_slot_time_bronze
    type: write
    source: v_slot_time_bronze_dqe
    write_target:
      create_table: true
      type: streaming_table
      catalog: "{catalog}"
      schema: "{bronze_schema}"
      table: "slot_time"

  - name: tst_slot_time_bronze_completeness
    type: test
    test_type: completeness
    source: v_slot_time_bronze_cleaned
    required_columns: [slot_time_key, _etl_timestamp]
    on_violation: warn
    test_id: "PERF-slot_time-B01"

  - name: tst_slot_time_bronze_uniqueness
    type: test
    test_type: uniqueness
    source: v_slot_time_bronze_cleaned
    columns: [slot_time_key]
    on_violation: warn
    test_id: "PERF-slot_time-B02"
